language: python
python:
  - "3.6"
install: "pip install -r requirements.txt"
script: publish.py
deploy:
  provider: pages
  skip_cleanup: true
  local_dir: public
  github_token: $GITHUB_TOKEN
  on:
    branch: master
